# 原版更新记录

iOS 仓库独立维护，原版功能通过 `cherry-pick -x` 引入，保留原作者、作者时间、提交说明和来源哈希。冲突按原版功能优先处理，同时保留 iOS 必需的原生桥接和安全区等适配。

## 2026-09-10

对照原版 `dev`：`dc8eab18496faa41d5c8e72d12927b5fcb29e845`。

| 原提交               | 处理                                                                   |
| -------------------- | ---------------------------------------------------------------------- |
| `251b3fb`            | 引入 QQ/网易云接口修复；保留 iOS 代理请求、试听和账号切换保护          |
| `7eff9c4`            | 引入动态弹窗层级                                                       |
| `dc8eab1`            | 引入搜索页“仅播放当前 / 播放全部”；保留移动布局单击播放                |
| `8539ffc`            | 引入原版加载页与动画，保留 iOS 启动错误提示、Siri 初始化               |
| `d3ef67f`            | 不引入上游仓库专用 CI，保留本仓库 IPA / Action 通道及关闭冒烟的配置    |
| `24bdf79`、`eb9bc28` | Electron 安装脚本，不引入 iOS 构建                                     |
| `311eae5`、`9180561` | Windows 缩略图工具栏及其合并提交，跳过                                 |
| `abb5a98`、`6f3e6c3` | Linux PipeWire 及其合并提交，跳过                                      |
| `d2ddbfc`、`7b03fb5` | 桌面原生媒体控制多歌手字段及其合并提交，iOS 使用独立原生媒体桥接，跳过 |
| `00bb426`            | Electron 安装脚本的合并提交，跳过                                      |

额外兼容修复单独提交：避免 QQ 并发鉴权失败使共享刷新失效。此记录表示按需引入，不表示整个原版分支已合并。
